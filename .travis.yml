language: node_js

node_js:
  - "10"

cache:
  directories:
    - node_modules

branches:
  only:
    - master
    - dev

script:
  - npm run build

after_success:
  - npm run test
  - npm run report-coverage
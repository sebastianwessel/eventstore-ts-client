language: node_js

node_js:
  - "10"

cache:
  directories:
    - node_modules
    - coverage

branches:
  only:
    - master
    - dev

script:
  - npm run test
  - npm run build

after_success:
  - npm install codecov -g
  - nyc report --reporter=json > coverage/coverage.json
  - codecov